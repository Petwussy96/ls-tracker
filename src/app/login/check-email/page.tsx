// Auth.js redirects here after a magic link has been emailed.
import { CheckEmailMessage } from "@/components/CheckEmailMessage";

export default function CheckEmailPage() {
  return <CheckEmailMessage />;
}
